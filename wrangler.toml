name = "moneyney"
compatibility_date = "2024-01-01"

# Pages 构建配置
pages_build_output_dir = "dist"

# 开发服务器配置（用于本地测试 Functions）
[dev]
port = 8788
local_protocol = "http"

# 环境变量（如果需要的话）
[vars]
# ENVIRONMENT = "production"
